/** Каждая строка словаря хранится парой [ru, uz] — так обе локали правятся в одном месте
 *  и ни один ключ не может «потеряться» в одном из языков. */
export type Dict = Record<string, readonly [string, string]>
